
import 'package:flutter/material.dart';

class Cargo_Type extends StatelessWidget {
  const Cargo_Type({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Container(
            margin: const EdgeInsets.only(top: 100), child: const Text("Trust The Process"))
      ],
    );
  }
}
